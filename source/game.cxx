#include "game.h"

Game::Game() {
    gameOptions.push_back('p'); //paper
    gameOptions.push_back('r'); //rock
    gameOptions.push_back('s'); //scissors
    //if we want more game options, we can add them here
}